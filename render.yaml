services:
  - type: web
    name: pneumonia-detection
    env: python
    plan: free
    pythonVersion: 3.13.13
    buildCommand: pip install -r requirements.txt
    startCommand: gunicorn -w 4 -b 0.0.0.0:$PORT "Flask Application.app:app"
